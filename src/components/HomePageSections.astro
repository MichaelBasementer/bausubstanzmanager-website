---
import { getFeaturesForLang } from "../config/features.config";
import { team } from "../config/team.config";
import type { SiteLang } from "../lib/lang";
import { translations } from "../config/i18n.config";

interface Props {
  lang: SiteLang;
  isDE: boolean;
}

const { lang, isDE } = Astro.props as Props;
const t = translations[lang];
const features = getFeaturesForLang(lang);

const workflowSteps = isDE
  ? [
      {
        num: "01",
        tag: "Desktop · 5 Minuten",
        title: "Projekt anlegen",
        body: "Grundriss hochladen, Projekt benennen, Team einrichten. BSM ist in Minuten einsatzbereit.",
        points: ["Eigene Grundrisse als Planvorlage", "Mehrere Gebäude & Etagen", "Team-Zuordnung und Rollen"],
      },
      {
        num: "02",
        tag: "Tablet · Offline-First",
        title: "Im Feld erfassen",
        body: "Auf der Baustelle arbeitest du offline — Proben, Sondierungen, Räume, Fotos. Alles landet direkt auf dem Grundriss.",
        points: ["Probenerfassung & Sondierungen", "Raumerfassung & Mengen", "Fotodokumentation mit GPS"],
      },
      {
        num: "03",
        tag: "1 Klick",
        title: "Synchronisieren",
        body: "Zurück im Büro: ein Klick, alles ist da. Verschlüsselt übertragen, DSGVO-konform, kein manuelles Übertragen.",
        points: ["AES-256 verschlüsselt", "Azure Deutschland (Frankfurt)", "Konfliktfreie Synchronisation"],
      },
      {
        num: "04",
        tag: "Desktop · Büro",
        title: "Fertigstellen & Berichten",
        body: "Mengenermittlung, Labor-Workflow, Entsorgungsnachweise und Abrechnungsprüfung, Planerstellung und Berichterstellung — alles in einem Werkzeug.",
        points: ["Mengenermittlung & Pläne", "Entsorgung mit Auto-Klassifizierung", "Abrechnungsprüfung & Berichte"],
      },
    ]
  : [
      {
        num: "01",
        tag: "Desktop · 5 minutes",
        title: "Create Project",
        body: "Upload floor plans, name the project, set up the team. BSM is ready in minutes.",
        points: ["Your own floor plans as templates", "Multiple buildings & floors", "Team assignment and roles"],
      },
      {
        num: "02",
        tag: "Tablet · Offline-First",
        title: "Capture in the Field",
        body: "On site you work fully offline — samples, drillings, rooms, photos. Everything lands directly on the floor plan.",
        points: ["Sample & drilling capture", "Room capture & quantities", "Photo documentation with GPS"],
      },
      {
        num: "03",
        tag: "1 Click",
        title: "Synchronize",
        body: "Back at the office: one click and everything is there. Encrypted transfer, GDPR-compliant, no manual copying.",
        points: ["AES-256 encrypted", "Azure Germany (Frankfurt)", "Conflict-free synchronization"],
      },
      {
        num: "04",
        tag: "Desktop · Office",
        title: "Finish & Report",
        body: "Quantity estimation, lab workflow, disposal manifests and invoice audits, plan creation and report generation — all in one tool.",
        points: ["Quantity estimation & plans", "Disposal with auto-classification", "Invoice audit & reports"],
      },
    ];

const trustBadges = ["VDI 6202", "AES-256", "SQLCipher", "Azure Frankfurt", "Offline-First", "DSGVO-konform"];
---

<!-- ══════════════════════════════════════════════════════════
     HERO
═══════════════════════════════════════════════════════════════ -->
<section class="hero-demo" id="hero">
  <div class="hero-bg" aria-hidden="true">
    <svg class="hero-grid-svg" viewBox="0 0 1600 900" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice">
      <rect width="1600" height="900" fill="#0d1b2a"/>
      <defs>
        <pattern id="hgrid-fine" width="24" height="24" patternUnits="userSpaceOnUse">
          <path d="M 24 0 L 0 0 0 24" fill="none" stroke="rgba(100,180,255,0.055)" stroke-width="0.5"/>
        </pattern>
        <pattern id="hgrid-major" width="120" height="120" patternUnits="userSpaceOnUse">
          <rect width="120" height="120" fill="url(#hgrid-fine)"/>
          <path d="M 120 0 L 0 0 0 120" fill="none" stroke="rgba(100,180,255,0.10)" stroke-width="1"/>
        </pattern>
      </defs>
      <rect width="1600" height="900" fill="url(#hgrid-major)"/>
    </svg>
    <div class="hero-glow hero-glow-1"></div>
    <div class="hero-glow hero-glow-2"></div>
  </div>

  <div class="hero-layout">
    <div class="hero-badge">
      <span class="hero-badge-dot"></span>
      {isDE ? "Materialprobe · Sondierung · Raumerfassung" : "Sample · Drilling · Room capture"}
    </div>

    <h1 class="hero-headline">{t.home.headline}</h1>
    <p class="hero-sub">{t.home.subheadline}</p>

    <div class="hero-ctas">
      <a class="btn btn-primary" href={`/${lang}/app`}>{t.home.cta}</a>
      <a class="btn btn-secondary" href={`/${lang}/roadmap`}>{t.home.ctaSecondary}</a>
    </div>

    <ul class="hero-chips">
      <li>Android · Tablet</li>
      <li>Windows · Desktop</li>
      <li>Offline-First</li>
    </ul>
  </div>
</section>

<!-- ══════════════════════════════════════════════════════════
     SCROLLYTELLING WORKFLOW
═══════════════════════════════════════════════════════════════ -->
<section class="workflow-section" id="workflow">
  <div class="workflow-inner">
    <!-- Left: scrolling steps -->
    <div class="workflow-steps" id="workflowSteps">
      {workflowSteps.map((step, i) => (
        <div class="workflow-step" data-step={i} id={`step-${i}`}>
          <div class="step-num">{step.num}</div>
          <span class="step-tag">{step.tag}</span>
          <h2 class="step-title">{step.title}</h2>
          <p class="step-body">{step.body}</p>
          <ul class="step-points">
            {step.points.map(p => <li>{p}</li>)}
          </ul>
        </div>
      ))}
    </div>

    <!-- Right: sticky visual panels -->
    <div class="workflow-visuals" id="workflowVisuals">
      <!-- Panel 0: Desktop / projekt anlegen -->
      <div class="workflow-panel" id="wp-0">
        <div class="device-frame device-desktop">
          <div class="device-bar">
            <span class="dev-dot"></span><span class="dev-dot"></span><span class="dev-dot"></span>
            <span class="dev-title">BSM Desktop — {isDE ? "Neues Projekt" : "New Project"}</span>
          </div>
          <div class="device-screen">
            <svg viewBox="0 0 420 260" xmlns="http://www.w3.org/2000/svg">
              <rect width="420" height="260" fill="#f4f1fe"/>
              <rect x="10" y="10" width="130" height="240" rx="6" fill="#fff" stroke="rgba(81,43,212,0.15)" stroke-width="1"/>
              <rect x="15" y="15" width="120" height="28" rx="4" fill="#512bd4"/>
              <text x="75" y="33" font-family="Inter,sans-serif" font-size="10" fill="#fff" text-anchor="middle" font-weight="700">{isDE ? "+ Neues Projekt" : "+ New Project"}</text>
              <rect x="15" y="52" width="120" height="22" rx="3" fill="rgba(81,43,212,0.08)" stroke="rgba(81,43,212,0.2)" stroke-width="1"/>
              <text x="75" y="67" font-family="Inter,sans-serif" font-size="9" fill="#512bd4" text-anchor="middle">Musterstraße 12</text>
              <rect x="150" y="10" width="260" height="240" rx="6" fill="#fff" stroke="rgba(81,43,212,0.1)" stroke-width="1"/>
              <rect x="160" y="18" width="240" height="140" rx="4" fill="#f4f1fe"/>
              <text x="280" y="92" font-family="'JetBrains Mono',monospace" font-size="11" fill="rgba(81,43,212,0.4)" text-anchor="middle">{isDE ? "Grundriss hochladen" : "Upload floor plan"}</text>
              <rect x="240" y="170" width="120" height="22" rx="4" fill="#512bd4"/>
              <text x="300" y="185" font-family="Inter,sans-serif" font-size="9" fill="#fff" text-anchor="middle" font-weight="700">{isDE ? "Projekt starten" : "Start Project"}</text>
            </svg>
          </div>
        </div>
      </div>

      <!-- Panel 1: Tablet / im Feld -->
      <div class="workflow-panel hidden" id="wp-1">
        <div class="device-frame device-tablet">
          <span class="device-camera" aria-hidden="true"></span>
          <div class="device-screen">
            <svg viewBox="0 0 320 240" xmlns="http://www.w3.org/2000/svg">
              <rect width="320" height="240" fill="#0d1b2a"/>
              <defs>
                <pattern id="tgrid" width="20" height="20" patternUnits="userSpaceOnUse">
                  <path d="M 20 0 L 0 0 0 20" fill="none" stroke="rgba(100,180,255,0.07)" stroke-width="0.5"/>
                </pattern>
              </defs>
              <rect width="320" height="240" fill="url(#tgrid)"/>
              <rect x="30" y="20" width="260" height="200" fill="none" stroke="rgba(100,180,255,0.6)" stroke-width="3"/>
              <line x1="140" y1="20" x2="140" y2="220" stroke="rgba(100,180,255,0.4)" stroke-width="2"/>
              <line x1="30" y1="120" x2="290" y2="120" stroke="rgba(100,180,255,0.4)" stroke-width="2"/>
              <!-- Animated markers -->
              <circle class="tmarker" cx="85" cy="70" r="9" fill="none" stroke="#e5402a" stroke-width="2"/>
              <line x1="80" y1="65" x2="90" y2="75" stroke="#e5402a" stroke-width="1.8" stroke-linecap="round"/>
              <line x1="90" y1="65" x2="80" y2="75" stroke="#e5402a" stroke-width="1.8" stroke-linecap="round"/>
              <text x="100" y="68" font-family="'JetBrains Mono',monospace" font-size="8" fill="#e5402a" font-weight="700">MP-01</text>
              <polygon class="tmarker" points="200,55 210,75 190,75" fill="none" stroke="#f59e0b" stroke-width="2"/>
              <text x="215" y="70" font-family="'JetBrains Mono',monospace" font-size="8" fill="#f59e0b" font-weight="700">SK-01</text>
              <rect class="tmarker" x="60" y="140" width="16" height="16" rx="3" fill="#1da9f2"/>
              <text x="68" y="152" font-family="'JetBrains Mono',monospace" font-size="9" fill="#fff" text-anchor="middle" font-weight="800">R</text>
              <text x="82" y="152" font-family="'JetBrains Mono',monospace" font-size="8" fill="#1da9f2" font-weight="700">R-01</text>
            </svg>
          </div>
        </div>
      </div>

      <!-- Panel 2: Sync animation -->
      <div class="workflow-panel hidden" id="wp-2">
        <div class="sync-visual">
          <div class="sync-device sync-tablet">
            <svg width="48" height="64" viewBox="0 0 48 64"><rect x="2" y="2" width="44" height="60" rx="6" fill="none" stroke="rgba(100,180,255,0.7)" stroke-width="2"/><rect x="8" y="8" width="32" height="44" rx="2" fill="rgba(100,180,255,0.08)"/><circle cx="24" cy="57" r="2" fill="rgba(100,180,255,0.4)"/></svg>
            <span>{isDE ? "Tablet" : "Tablet"}</span>
          </div>
          <div class="sync-dots" aria-hidden="true">
            <span class="sdot"></span><span class="sdot"></span><span class="sdot"></span>
          </div>
          <div class="sync-cloud">
            <svg width="48" height="36" viewBox="0 0 48 36"><path d="M36 30H12a10 10 0 0 1-2-19.8A14 14 0 0 1 38 18a8 8 0 0 1-2 12z" fill="none" stroke="rgba(124,92,232,0.9)" stroke-width="2"/></svg>
            <span>Azure DE</span>
          </div>
          <div class="sync-dots" aria-hidden="true">
            <span class="sdot"></span><span class="sdot"></span><span class="sdot"></span>
          </div>
          <div class="sync-device sync-desktop">
            <svg width="64" height="48" viewBox="0 0 64 48"><rect x="2" y="2" width="60" height="38" rx="4" fill="none" stroke="rgba(100,180,255,0.7)" stroke-width="2"/><rect x="8" y="8" width="48" height="26" rx="2" fill="rgba(100,180,255,0.08)"/><rect x="22" y="42" width="20" height="4" rx="2" fill="rgba(100,180,255,0.3)"/></svg>
            <span>Desktop</span>
          </div>
          <div class="sync-badge">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none"><rect x="3" y="11" width="18" height="11" rx="2" stroke="rgba(100,180,255,0.8)" stroke-width="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4" stroke="rgba(100,180,255,0.8)" stroke-width="2" stroke-linecap="round"/></svg>
            {isDE ? "Ende-zu-Ende verschlüsselt" : "End-to-end encrypted"}
          </div>
        </div>
      </div>

      <!-- Panel 3: Desktop / büro -->
      <div class="workflow-panel hidden" id="wp-3">
        <div class="device-frame device-desktop">
          <div class="device-bar">
            <span class="dev-dot"></span><span class="dev-dot"></span><span class="dev-dot"></span>
            <span class="dev-title">BSM Desktop — {isDE ? "Auswertung" : "Analysis"}</span>
          </div>
          <div class="device-screen">
            <svg viewBox="0 0 420 260" xmlns="http://www.w3.org/2000/svg">
              <rect width="420" height="260" fill="#f4f1fe"/>
              <rect x="10" y="10" width="130" height="240" rx="6" fill="#fff" stroke="rgba(81,43,212,0.12)" stroke-width="1"/>
              <rect x="15" y="15" width="120" height="20" rx="3" fill="rgba(81,43,212,0.08)"/>
              <text x="75" y="28" font-family="Inter,sans-serif" font-size="9" fill="#512bd4" text-anchor="middle">{isDE ? "Probenübersicht" : "Sample Overview"}</text>
              <rect x="15" y="42" width="120" height="18" rx="3" fill="rgba(81,43,212,0.04)"/>
              <text x="75" y="54" font-family="Inter,sans-serif" font-size="8" fill="#6c6791" text-anchor="middle">{isDE ? "Mengenermittlung" : "Quantities"}</text>
              <rect x="15" y="66" width="120" height="18" rx="3" fill="rgba(81,43,212,0.04)"/>
              <text x="75" y="78" font-family="Inter,sans-serif" font-size="8" fill="#6c6791" text-anchor="middle">{isDE ? "Labor-Workflow" : "Lab Workflow"}</text>
              <rect x="15" y="90" width="120" height="18" rx="3" fill="rgba(81,43,212,0.04)"/>
              <text x="75" y="102" font-family="Inter,sans-serif" font-size="8" fill="#6c6791" text-anchor="middle">{isDE ? "Berichte & Export" : "Reports & Export"}</text>
              <rect x="150" y="10" width="260" height="115" rx="6" fill="#fff" stroke="rgba(81,43,212,0.1)" stroke-width="1"/>
              <text x="280" y="26" font-family="Inter,sans-serif" font-size="9" fill="#1e1742" text-anchor="middle" font-weight="700">{isDE ? "Proben (5)" : "Samples (5)"}</text>
              <rect x="158" y="32" width="244" height="1" fill="rgba(81,43,212,0.1)"/>
              {[["MP-01","Linoleumkleber","PK3"],["MP-02","Putz","PK1"],["SK-01","Kernbohrung","—"],["R-01","Büro 1","—"]].map((row, ri) => (
                <g transform={`translate(0,${44 + ri * 22})`}>
                  <text x="162" y="10" font-family="'JetBrains Mono',monospace" font-size="8" fill="#512bd4">{row[0]}</text>
                  <text x="210" y="10" font-family="Inter,sans-serif" font-size="8" fill="#1e1742">{row[1]}</text>
                  <rect x="334" y="2" width="28" height="12" rx="3" fill={ri < 2 ? "rgba(229,64,42,0.15)" : "rgba(81,43,212,0.1)"}/>
                  <text x="348" y="11" font-family="Inter,sans-serif" font-size="7" fill={ri < 2 ? "#e5402a" : "#512bd4"} text-anchor="middle">{row[2]}</text>
                </g>
              ))}
              <rect x="150" y="134" width="125" height="116" rx="6" fill="#fff" stroke="rgba(81,43,212,0.1)" stroke-width="1"/>
              <text x="212" y="150" font-family="Inter,sans-serif" font-size="9" fill="#1e1742" text-anchor="middle" font-weight="700">{isDE ? "Mengen" : "Quantities"}</text>
              <rect x="158" y="158" width="109" height="16" rx="3" fill="rgba(81,43,212,0.08)"/>
              <text x="212" y="170" font-family="Inter,sans-serif" font-size="8" fill="#512bd4" text-anchor="middle">{isDE ? "Büro 1: 24 m²" : "Office 1: 24 m²"}</text>
              <rect x="158" y="180" width="109" height="16" rx="3" fill="rgba(81,43,212,0.05)"/>
              <text x="212" y="192" font-family="Inter,sans-serif" font-size="8" fill="#512bd4" text-anchor="middle">{isDE ? "Flur: 38 m²" : "Corridor: 38 m²"}</text>
              <rect x="285" y="134" width="125" height="116" rx="6" fill="#512bd4"/>
              <text x="347" y="155" font-family="Inter,sans-serif" font-size="9" fill="#fff" text-anchor="middle" font-weight="700">{isDE ? "PDF Export" : "PDF Export"}</text>
              <rect x="298" y="163" width="99" height="50" rx="4" fill="rgba(255,255,255,0.12)"/>
              <text x="347" y="190" font-family="Inter,sans-serif" font-size="8" fill="rgba(255,255,255,0.8)" text-anchor="middle">{isDE ? "Bericht bereit" : "Report ready"}</text>
              <rect x="298" y="220" width="99" height="18" rx="3" fill="rgba(255,255,255,0.2)"/>
              <text x="347" y="232" font-family="Inter,sans-serif" font-size="8" fill="#fff" text-anchor="middle" font-weight="700">↓ Download</text>
            </svg>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════════════════════════
     9 MODULE GRID
═══════════════════════════════════════════════════════════════ -->
<section class="modules-section" id="module">
  <div class="container-custom">
    <div class="modules-header">
      <span class="section-eyebrow">{isDE ? "Funktionsumfang" : "Feature Set"}</span>
      <h2 class="section-title">{isDE ? "Alles was du brauchst." : "Everything you need."}</h2>
      <p class="section-lead">
        {isDE
          ? "Neun spezialisierte Workflows — vom ersten Marker auf der Baustelle bis zum fertigen Laborbericht."
          : "Nine specialised workflows — from the first marker on site to the finished lab report."}
      </p>
    </div>
    <div class="modules-grid">
      {features.map((f) => (
        <a href={`/${lang}/app#feature-${f.id}`} class="module-card scroll-reveal">
          <div class="module-icon">{f.icon}</div>
          <div class="module-platform" data-platform={f.platform}>
            {f.platform === "mobile" ? (isDE ? "Tablet" : "Tablet")
             : f.platform === "desktop" ? "Desktop"
             : (isDE ? "Tablet & Desktop" : "Tablet & Desktop")}
          </div>
          <h3>{f.title}</h3>
          <p>{f.description}</p>
          <span class="module-more">{isDE ? "Details →" : "Details →"}</span>
        </a>
      ))}
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════════════════════════
     PLNMAKER ADD-ON HIGHLIGHT
═══════════════════════════════════════════════════════════════ -->
<section class="plnmaker-section" id="plnmaker">
  <div class="container-custom">
    <div class="plnmaker-card scroll-reveal">
      <div class="plnmaker-copy">
        <div class="plnmaker-tags">
          <span class="tag tag-dark">{t.plnmaker.eyebrow}</span>
          <span class="plnmaker-badge">{t.plnmaker.badge}</span>
        </div>
        <h2>{t.plnmaker.title}</h2>
        <p class="plnmaker-lead">{t.plnmaker.lead}</p>
        <p class="plnmaker-body">{t.plnmaker.body}</p>
        <a class="plnmaker-cta" href="https://kellerer-engineering.de/#projekte" target="_blank" rel="noopener">
          {t.plnmaker.cta}
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none"><path d="M13.5 6H5.25A2.25 2.25 0 0 0 3 8.25v10.5A2.25 2.25 0 0 0 5.25 21h10.5A2.25 2.25 0 0 0 18 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
      </div>
      <ul class="plnmaker-bullets">
        {t.plnmaker.bullets.map((b: string) => (
          <li>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none"><path d="m4.5 12.75 6 6 9-13.5" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
            {b}
          </li>
        ))}
      </ul>
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════════════════════════
     SYNC / TRUST STRIP
═══════════════════════════════════════════════════════════════ -->
<section class="trust-section">
  <div class="container-custom">
    <div class="trust-visual" aria-hidden="true">
      <div class="trust-node">
        <svg width="40" height="54" viewBox="0 0 40 54"><rect x="1" y="1" width="38" height="52" rx="5" fill="none" stroke="rgba(196,181,244,0.6)" stroke-width="2"/><rect x="6" y="6" width="28" height="38" rx="2" fill="rgba(124,92,232,0.12)"/><circle cx="20" cy="49" r="2" fill="rgba(196,181,244,0.4)"/></svg>
        <span>Tablet</span>
      </div>
      <div class="trust-line" aria-hidden="true">
        <span class="tl-dot"></span><span class="tl-dot"></span><span class="tl-dot"></span>
      </div>
      <div class="trust-node trust-cloud">
        <svg width="48" height="34" viewBox="0 0 48 34"><path d="M36 28H12A10 10 0 0 1 10 8.2 14 14 0 0 1 38 16a8 8 0 0 1-2 12z" fill="none" stroke="rgba(124,92,232,0.9)" stroke-width="2"/></svg>
        <span>Azure DE</span>
      </div>
      <div class="trust-line" aria-hidden="true">
        <span class="tl-dot"></span><span class="tl-dot"></span><span class="tl-dot"></span>
      </div>
      <div class="trust-node">
        <svg width="56" height="42" viewBox="0 0 56 42"><rect x="1" y="1" width="54" height="34" rx="4" fill="none" stroke="rgba(196,181,244,0.6)" stroke-width="2"/><rect x="6" y="6" width="44" height="24" rx="2" fill="rgba(124,92,232,0.12)"/><rect x="18" y="37" width="20" height="4" rx="2" fill="rgba(196,181,244,0.3)"/></svg>
        <span>Desktop</span>
      </div>
    </div>

    <div class="trust-copy">
      <h2>{isDE ? "Deine Daten. Verschlüsselt. Sicher. Immer verfügbar." : "Your data. Encrypted. Secure. Always available."}</h2>
      <p>
        {isDE
          ? "BSM arbeitet vollständig offline — auf Tablet und Desktop. Wenn du synchronisierst, werden alle Daten Ende-zu-Ende verschlüsselt übertragen."
          : "BSM works fully offline — on tablet and desktop. When you sync, all data is transferred end-to-end encrypted."}
      </p>
    </div>

    <div class="trust-badges">
      {trustBadges.map(b => <span class="trust-badge">{b}</span>)}
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════════════════════════
     TEAM
═══════════════════════════════════════════════════════════════ -->
<section class="team-section" id="team">
  <div class="container-custom">
    <div class="modules-header">
      <span class="section-eyebrow">{t.team.title}</span>
      <h2 class="section-title">{t.team.intro}</h2>
    </div>
    <div class="team-grid">
      {team.map((member) => (
        <div class="team-card scroll-reveal">
          <div class="team-avatar" aria-hidden="true">{member.name.slice(0, 2).toUpperCase()}</div>
          <h3>{member.name}</h3>
          <p class="team-role">{member.title}</p>
          <p class="team-bio">{member.bio}</p>
          {member.email && <a class="team-email" href={`mailto:${member.email}`}>{member.email}</a>}
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════════════════════════
     FINAL CTA
═══════════════════════════════════════════════════════════════ -->
<section class="final-cta-section">
  <div class="container-custom final-cta-inner">
    <h2>{isDE ? "Bereit für den ersten Grundriss?" : "Ready for your first floor plan?"}</h2>
    <p>
      {isDE
        ? "Schau dir die App im Detail an oder wirf einen Blick auf die Roadmap, um zu sehen, was als Nächstes kommt."
        : "Take a closer look at the app, or check the roadmap to see what's coming next."}
    </p>
    <div class="final-cta-actions">
      <a class="btn btn-primary" href={`/${lang}/app`}>{t.home.cta}</a>
      <a class="btn btn-secondary" href={`/${lang}/roadmap`}>{t.home.ctaSecondary}</a>
      <a class="final-cta-mail" href="mailto:support@bausubstanzmanager.de">support@bausubstanzmanager.de</a>
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════════════════════════
     STYLES
═══════════════════════════════════════════════════════════════ -->
<style>
  /* ── Shared ── */
  .section-eyebrow { display: block; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(124,92,232,0.85); margin-bottom: 0.75rem; }
  .section-title { font-size: clamp(1.8rem, 3.5vw, 2.8rem); font-weight: 900; color: #1e1742; line-height: 1.1; margin: 0 0 1rem; letter-spacing: -0.02em; }
  .section-lead { font-size: 1rem; color: #6c6791; line-height: 1.75; max-width: 600px; margin: 0; }
  .scroll-reveal { opacity: 0; transform: translateY(18px); transition: opacity 0.55s ease, transform 0.55s ease; }
  .scroll-reveal.visible { opacity: 1; transform: none; }

  /* ── Hero ── */
  .hero-demo { position: relative; background: #0d1b2a; overflow: hidden; padding: 9rem 0 6rem; }
  .hero-bg { position: absolute; inset: 0; z-index: 0; }
  .hero-grid-svg { width: 100%; height: 100%; }
  .hero-glow { position: absolute; border-radius: 50%; filter: blur(90px); pointer-events: none; }
  .hero-glow-1 { top: -8rem; left: 50%; transform: translateX(-50%); width: 34rem; height: 34rem; background: rgba(81,43,212,0.28); }
  .hero-glow-2 { bottom: -10rem; right: 8%; width: 26rem; height: 26rem; background: rgba(29,169,242,0.14); }
  .hero-layout { position: relative; z-index: 2; width: 100%; max-width: 780px; margin: 0 auto; padding: 0 1.5rem; display: flex; flex-direction: column; align-items: center; text-align: center; gap: 1.5rem; }

  .hero-badge { display: inline-flex; align-items: center; gap: 0.5rem; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.06em; text-transform: uppercase; color: rgba(200,220,255,0.8); background: rgba(100,180,255,0.08); border: 1px solid rgba(100,180,255,0.2); border-radius: 999px; padding: 0.5rem 1.1rem; }
  .hero-badge-dot { width: 7px; height: 7px; border-radius: 50%; background: #7c5ce8; box-shadow: 0 0 0 4px rgba(124,92,232,0.25); flex-shrink: 0; }
  .hero-headline { font-size: clamp(2.1rem, 5vw, 3.5rem); font-weight: 900; color: #fff; line-height: 1.12; margin: 0; letter-spacing: -0.03em; }
  .hero-sub { font-size: clamp(1rem, 1.6vw, 1.15rem); color: rgba(200,220,255,0.75); line-height: 1.7; margin: 0; max-width: 560px; }
  .hero-ctas { display: flex; gap: 0.75rem; flex-wrap: wrap; justify-content: center; padding-top: 0.25rem; }
  .btn-secondary { background: transparent; color: rgba(196,181,244,0.85); border: 1.5px solid rgba(124,92,232,0.35); border-radius: 8px; padding: 0.7rem 1.4rem; font-size: 0.88rem; font-weight: 700; text-decoration: none; display: inline-flex; align-items: center; transition: all 0.2s; }
  .btn-secondary:hover { background: rgba(124,92,232,0.15); border-color: rgba(124,92,232,0.7); color: #fff; }
  .hidden { display: none !important; }

  /* ── Workflow scrollytelling ── */
  .workflow-section { background: #110e26; }
  .workflow-inner { display: grid; grid-template-columns: 1fr 1fr; min-height: 400vh; position: relative; max-width: 1300px; margin: 0 auto; padding: 0 1.5rem; }
  @media (max-width: 900px) { .workflow-inner { grid-template-columns: 1fr; min-height: auto; } }
  .workflow-steps { padding: 8rem 3rem 8rem 0; display: flex; flex-direction: column; gap: 0; }
  @media (max-width: 900px) { .workflow-steps { padding: 4rem 0; } }
  .workflow-step { min-height: 100vh; display: flex; flex-direction: column; justify-content: center; padding: 2rem 0; opacity: 0.3; transition: opacity 0.4s ease; }
  .workflow-step.is-active { opacity: 1; }
  .step-num { font-family: 'JetBrains Mono', monospace; font-size: 3.5rem; font-weight: 900; color: rgba(124,92,232,0.15); line-height: 1; margin-bottom: 0.5rem; }
  .step-tag { font-size: 0.72rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(124,92,232,0.75); display: block; margin-bottom: 1rem; }
  .step-title { font-size: clamp(1.8rem, 3vw, 2.6rem); font-weight: 900; color: #fff; line-height: 1.1; margin: 0 0 1rem; letter-spacing: -0.02em; }
  .step-body { font-size: 1rem; color: rgba(196,181,244,0.75); line-height: 1.75; margin: 0 0 1.5rem; max-width: 480px; }
  .step-points { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0.6rem; }
  .step-points li { font-size: 0.9rem; color: rgba(196,181,244,0.65); padding-left: 1.4rem; position: relative; }
  .step-points li::before { content: "→"; position: absolute; left: 0; color: #7c5ce8; font-weight: 700; }

  .workflow-visuals { position: sticky; top: 64px; height: calc(100vh - 64px); display: flex; align-items: center; justify-content: center; padding: 2rem 0 2rem 3rem; }
  @media (max-width: 900px) { .workflow-visuals { display: none; } }
  .workflow-panel { width: 100%; transition: opacity 0.5s ease, transform 0.5s ease; }
  .workflow-panel.hidden { display: none; }

  /* Device frames */
  .device-frame { border-radius: 16px; overflow: hidden; box-shadow: 0 30px 80px rgba(0,0,0,0.5), 0 0 0 1px rgba(255,255,255,0.06); }
  .device-desktop { background: linear-gradient(145deg, #1a1530, #0e0b20); }
  .device-bar { background: #1e1742; padding: 0.6rem 0.85rem; display: flex; align-items: center; gap: 0.4rem; }
  .dev-dot { width: 10px; height: 10px; border-radius: 50%; background: rgba(255,255,255,0.15); display: inline-block; }
  .dev-title { font-family: monospace; font-size: 0.72rem; color: rgba(255,255,255,0.4); margin-left: 0.6rem; }
  .device-screen { padding: 0.75rem; }
  .device-screen svg { width: 100%; display: block; border-radius: 8px; }
  .device-tablet { background: linear-gradient(145deg, #161225, #0b0a14); padding: 1rem 0.85rem; position: relative; border-radius: 28px; }
  .device-camera { position: absolute; top: 0.5rem; left: 50%; transform: translateX(-50%); width: 50px; height: 5px; border-radius: 999px; background: rgba(255,255,255,0.1); }

  /* Sync visual */
  .sync-visual { display: flex; flex-direction: column; align-items: center; gap: 1.5rem; padding: 3rem 2rem; }
  .sync-device { display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .sync-device span { font-size: 0.72rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(196,181,244,0.6); }
  .sync-cloud { display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .sync-cloud span { font-size: 0.72rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(124,92,232,0.8); }
  .sync-dots { display: flex; gap: 0.5rem; align-items: center; }
  .sdot { width: 6px; height: 6px; border-radius: 50%; background: rgba(124,92,232,0.5); animation: dotFlow 1.4s ease-in-out infinite; }
  .sdot:nth-child(2) { animation-delay: 0.25s; }
  .sdot:nth-child(3) { animation-delay: 0.5s; }
  @keyframes dotFlow { 0%,100% { opacity:0.3; transform: scale(0.8); } 50% { opacity:1; transform: scale(1.1); } }
  .sync-badge { display: flex; align-items: center; gap: 0.4rem; font-size: 0.72rem; font-weight: 700; color: rgba(100,180,255,0.65); border: 1px solid rgba(100,180,255,0.2); border-radius: 999px; padding: 0.35rem 0.85rem; margin-top: 0.5rem; }

  /* ── Module grid ── */
  .modules-section { background: #fff; padding: 7rem 0; }
  .modules-header { text-align: center; margin-bottom: 4rem; }
  .modules-header .section-lead { margin: 0.75rem auto 0; }
  .modules-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; }
  @media (max-width: 900px) { .modules-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 560px) { .modules-grid { grid-template-columns: 1fr; } }
  .module-card { display: flex; flex-direction: column; gap: 0.6rem; background: #fff; border-radius: 16px; padding: 1.75rem; border: 1px solid rgba(81,43,212,0.1); text-decoration: none; color: inherit; transition: all 0.25s ease; box-shadow: 0 2px 12px rgba(81,43,212,0.06); }
  .module-card:hover { transform: translateY(-4px); box-shadow: 0 16px 48px rgba(81,43,212,0.14); border-color: rgba(81,43,212,0.25); }
  .module-icon { font-size: 1.8rem; line-height: 1; }
  .module-platform { font-size: 0.66rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; padding: 0.2rem 0.55rem; border-radius: 999px; display: inline-block; width: fit-content; }
  .module-platform[data-platform="mobile"] { background: rgba(29,169,242,0.12); color: #1da9f2; }
  .module-platform[data-platform="desktop"] { background: rgba(81,43,212,0.1); color: #512bd4; }
  .module-platform[data-platform="both"] { background: rgba(34,197,94,0.1); color: #16a34a; }
  .module-card h3 { font-size: 1rem; font-weight: 800; color: #1e1742; margin: 0; }
  .module-card p { font-size: 0.875rem; color: #6c6791; line-height: 1.65; margin: 0; flex: 1; }
  .module-more { font-size: 0.78rem; font-weight: 700; color: #512bd4; letter-spacing: 0.04em; margin-top: auto; }

  /* ── Trust section ── */
  .trust-section { background: #0d1b2a; padding: 7rem 0; text-align: center; }
  .trust-visual { display: flex; align-items: center; justify-content: center; gap: 1.5rem; flex-wrap: wrap; margin-bottom: 3rem; }
  .trust-node { display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
  .trust-node span { font-size: 0.68rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(196,181,244,0.55); }
  .trust-cloud span { color: rgba(124,92,232,0.8); }
  .trust-line { display: flex; gap: 0.4rem; align-items: center; }
  .tl-dot { width: 6px; height: 6px; border-radius: 50%; background: rgba(124,92,232,0.45); animation: dotFlow 1.6s ease-in-out infinite; }
  .tl-dot:nth-child(2) { animation-delay: 0.3s; }
  .tl-dot:nth-child(3) { animation-delay: 0.6s; }
  .trust-copy { max-width: 640px; margin: 0 auto 3rem; }
  .trust-copy h2 { font-size: clamp(1.4rem, 2.5vw, 2rem); font-weight: 900; color: #fff; line-height: 1.2; margin: 0 0 1rem; letter-spacing: -0.02em; }
  .trust-copy p { font-size: 0.95rem; color: rgba(196,181,244,0.65); line-height: 1.75; margin: 0; }
  .trust-badges { display: flex; gap: 0.6rem; flex-wrap: wrap; justify-content: center; }
  .trust-badge { font-size: 0.72rem; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; padding: 0.4rem 0.9rem; border-radius: 999px; border: 1px solid rgba(124,92,232,0.3); color: rgba(196,181,244,0.7); }

  /* ── Hero platform chips ── */
  .hero-chips { list-style: none; display: flex; flex-wrap: wrap; justify-content: center; gap: 0.5rem; margin: 0; padding: 0; }
  .hero-chips li { font-size: 0.72rem; font-weight: 600; color: rgba(200,220,255,0.7); background: rgba(100,180,255,0.07); border: 1px solid rgba(100,180,255,0.16); border-radius: 999px; padding: 0.3rem 0.75rem; }

  /* ── PLNMaker add-on highlight ── */
  .plnmaker-section { background: #fff; padding: 2rem 0 5rem; }
  .plnmaker-card { display: grid; grid-template-columns: 1.4fr 1fr; gap: 2.5rem; background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark)); border-radius: var(--radius-lg); padding: 3rem; box-shadow: var(--shadow-elevated); }
  @media (max-width: 900px) { .plnmaker-card { grid-template-columns: 1fr; padding: 2rem; } }
  .plnmaker-tags { display: flex; align-items: center; gap: 0.6rem; flex-wrap: wrap; margin-bottom: 1.25rem; }
  .plnmaker-badge { font-size: 0.7rem; font-weight: 700; letter-spacing: 0.06em; color: rgba(255,255,255,0.75); }
  .plnmaker-copy h2 { font-size: clamp(1.5rem, 2.6vw, 2.1rem); font-weight: 900; color: #fff; line-height: 1.15; margin: 0 0 1rem; letter-spacing: -0.02em; }
  .plnmaker-lead { font-size: 1.02rem; font-weight: 600; color: rgba(255,255,255,0.92); line-height: 1.6; margin: 0 0 0.85rem; }
  .plnmaker-body { font-size: 0.92rem; color: rgba(232,226,252,0.82); line-height: 1.75; margin: 0 0 1.5rem; max-width: 520px; }
  .plnmaker-cta { display: inline-flex; align-items: center; gap: 0.4rem; font-size: 0.88rem; font-weight: 700; color: #fff; text-decoration: none; border-bottom: 1.5px solid rgba(255,255,255,0.4); padding-bottom: 0.15rem; transition: border-color 0.2s; }
  .plnmaker-cta:hover { border-color: #fff; }
  .plnmaker-bullets { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 0.9rem; align-self: center; }
  .plnmaker-bullets li { display: flex; align-items: flex-start; gap: 0.6rem; font-size: 0.9rem; font-weight: 600; color: #fff; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.16); border-radius: 12px; padding: 0.75rem 1rem; }
  .plnmaker-bullets li svg { flex-shrink: 0; margin-top: 0.15rem; color: rgba(255,255,255,0.85); }

  /* ── Team ── */
  .team-section { background: var(--color-secondary); padding: 7rem 0; }
  .team-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.25rem; max-width: 700px; margin: 0 auto; }
  .team-card { background: #fff; border-radius: var(--radius-md); padding: 2rem; text-align: center; box-shadow: var(--shadow-card); border: 1px solid var(--color-border); }
  .team-avatar { width: 64px; height: 64px; border-radius: 50%; background: linear-gradient(135deg, var(--color-primary-light), var(--color-primary)); color: #fff; display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 1.1rem; margin: 0 auto 1rem; }
  .team-card h3 { font-size: 1.1rem; font-weight: 800; color: var(--color-tertiary); margin: 0 0 0.2rem; }
  .team-role { font-size: 0.82rem; font-weight: 700; color: var(--color-primary); margin: 0 0 0.85rem; }
  .team-bio { font-size: 0.88rem; color: var(--color-accent); line-height: 1.65; margin: 0 0 0.85rem; }
  .team-email { font-size: 0.82rem; font-weight: 600; color: var(--color-primary); text-decoration: none; }
  .team-email:hover { text-decoration: underline; }

  /* ── Final CTA ── */
  .final-cta-section { background: var(--color-header-gray); padding: 6rem 0; }
  .final-cta-inner { text-align: center; max-width: 640px; }
  .final-cta-inner h2 { font-size: clamp(1.5rem, 2.6vw, 2.1rem); font-weight: 900; color: #fff; margin: 0 0 1rem; letter-spacing: -0.02em; }
  .final-cta-inner p { font-size: 0.98rem; color: rgba(196,181,244,0.75); line-height: 1.7; margin: 0 0 2rem; }
  .final-cta-actions { display: flex; align-items: center; justify-content: center; gap: 1rem; flex-wrap: wrap; }
  .final-cta-mail { font-size: 0.85rem; font-weight: 600; color: rgba(196,181,244,0.7); text-decoration: none; }
  .final-cta-mail:hover { color: #fff; }
</style>

<!-- ══════════════════════════════════════════════════════════
     SCRIPTS
═══════════════════════════════════════════════════════════════ -->
<script>
  // ── Scrollytelling ─────────────────────────────────────
  const steps = document.querySelectorAll<HTMLElement>('.workflow-step');
  const panels = document.querySelectorAll<HTMLElement>('.workflow-panel');

  function showPanel(idx: number) {
    panels.forEach((p, i) => {
      if (i === idx) { p.classList.remove('hidden'); }
      else { p.classList.add('hidden'); }
    });
    steps.forEach((s, i) => s.classList.toggle('is-active', i === idx));
  }

  const stepObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const idx = parseInt((entry.target as HTMLElement).dataset.step || '0', 10);
          showPanel(idx);
        }
      });
    },
    { threshold: 0.5, rootMargin: '-10% 0px -40% 0px' }
  );

  steps.forEach(step => stepObserver.observe(step));
  showPanel(0);

  // ── Scroll reveal for module cards ─────────────────────
  const revealObs = new IntersectionObserver(
    (entries) => entries.forEach(e => e.isIntersecting && e.target.classList.add('visible')),
    { threshold: 0.1 }
  );
  document.querySelectorAll('.scroll-reveal').forEach(el => revealObs.observe(el));
</script>
